<%= render 'shared/request' %>

<% if ['accepted', 'complete'].include?(@request.status?) %>
	<%= link_to 'メッセージ', user_request_messages_path(current_user,@request) %>
<% end %>

<%= link_to '一覧に戻る', requests_path %>